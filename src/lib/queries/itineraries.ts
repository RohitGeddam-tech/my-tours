export async function getItineraries(destinationId: string) {
    // Supabase query for itineraries
    return [];
}

export async function getItineraryById(id: string) {
    // Supabase query for single itinerary
    return null;
}
