export async function getDestinations() {
    // Supabase query for destinations
    return [];
}

export async function getDestinationBySlug(slug: string) {
    // Supabase query for single destination
    return null;
}
